server {
    listen 4000;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular client-side routing: orice rută necunoscută cade pe index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
